<%= form_with(model: cycle_ecole, local: true) do |form| %>
  <% if cycle_ecole.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cycle_ecole.errors.count, "error") %> prohibited this cycle_ecole from being saved:</h2>

      <ul>
        <% cycle_ecole.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_field :detail %>
  </div>

  <div class="field">
    <%= form.label :structure_id %>
    <%= form.collection_select(:structure_id, Structure.all, :id, :name, :include_blank => "Please select a structure...") %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
